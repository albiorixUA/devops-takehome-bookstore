# Copy to terraform.tfvars (which is gitignored) and adjust to your VPC.
region = "us-east-1"

# Replace with your VPC's CIDR or a bastion subnet. 0.0.0.0/0 is rejected by validation.
allowed_cidrs = ["10.0.0.0/16"]
